#include<stdio.h>
int main()
{
int a,j;
unsigned long long factorial=1;
printf("enter an integer:");
scanf("%d",&n);
if(a<0)
printf("error!doesn't exist factorial number in negative.");
else
[
for(j=1,j<a;++j)
{
factorial *=j;
}
printf("factorial of %d="llu",a,factorial);
}
return 0;
}
